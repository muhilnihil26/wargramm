// Mock data has been removed. The app now uses only real database content.
// Kept as an empty module to avoid breaking any stray imports during refactors.

export const stories: never[] = [];
export const posts: never[] = [];
export const reels: never[] = [];
export const exploreImages: never[] = [];
